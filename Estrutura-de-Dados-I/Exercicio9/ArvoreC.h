//
// Arvore.h
//
// Criado por Gabrielly Cariman em 29/08/21.
//
// Implementação de uma árvore binária. Ou ela está vazia, ou um nó raiz com duas subárvores
//

#ifndef ArvoreC_h
#define ArvoreC_h
/*
#include <stdbool.h>

typedef struct arv Arv;

#include "Aluno.h"

//Cria uma árvore vazia
Arv *arv_criavazia();

// Cria uma árvore com a informação do nó raiz al, e com subárvore esquerda e e subárvore direira d
Arv *arv_cria(Aluno *al, Arv *e, Arv *d); //Recebe a informação do nó, que nesse caso é um Aluno e as arvores da esquerda e direita

//retorna true se a árvore estiver vazia e false caso contrário
bool arv_vazia(Arv *a); //rebe a raíz da árvore

//libera o espaço de memória ocupado pela árvore a
Arv *arv_libera(Arv *a); //rebe a raíz da árvore

//indica a ocorrência (1) ou não (0) do Aluno com o nome informador
bool arv_pertence(Arv *a, char *nome); // Recebe a raíz a e uma string nome que é a chave de busca para verificar se ele está na árvore ou não

//imprime as informações dos nós da árvore
void arv_imprime(Arv *a); //rebe a raíz da árvore

//Retorna a altura da árvore
int arv_altura(Arv *a);

//retorna a quantidade de folhas de uma árvore binaria
int folhas(Arv *a);

//retorna o número de ocorrencias de um dado nome de Aluno na árvore
int ocorrencias(Arv *a, char *nome);

//imprime as informações dos nós da árvore
//void arv_imprime_graficamente(Arv *a);
*/

#endif /*Arvore_h*/
